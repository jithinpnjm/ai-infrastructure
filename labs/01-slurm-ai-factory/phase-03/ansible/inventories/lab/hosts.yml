---
# Phase 03 lab inventory.
#
# GPU workers provisioned in Phase 04.
# Keep this inventory free of cloud credentials and other secrets.
all:
  children:
    gpu_workers:
      hosts:
        l40-node-01:
          ansible_host: 10.0.0.57
        l40-node-02:
          ansible_host: 10.0.0.36
      vars:
        ansible_user: jithin
        ansible_ssh_private_key_file: ~/.ssh/nvidia-lab
