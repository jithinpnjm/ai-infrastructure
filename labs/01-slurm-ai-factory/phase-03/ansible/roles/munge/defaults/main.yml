---
munge_package: munge
munge_service_name: munge
munge_key_destination: /etc/munge/munge.key

# This path exists only on the Ansible controller and is ignored by Git.
# Copy the trusted cluster key here before running the convergence playbook.
munge_key_source: "{{ playbook_dir }}/../secrets/munge.key"
